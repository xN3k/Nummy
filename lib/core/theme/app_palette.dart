import 'package:flutter/material.dart';

class AppPalette {
  // Primary Color
  static const Color primaryColor = Color(
    0xFF6B45BC,
  ); // Purple - Main brand color

  // Secondary Color
  static const Color secondaryColor = Color(
    0xFFFFB800,
  ); // Yellow - Complementary color

  // Accent Color
  static const Color accentColor = Color(
    0xFF1D1D1D,
  ); // Black - For highlights or contrast

  // Neutral/Grey Color
  static const Color neutralTextColor = Color(
    0xFFB0B0B0,
  ); // Slightly darker grey// Light Grey - For backgrounds or dividers

  static const Color fillColor = Color(0xFFf1f1f3);

  // Background Color (Pure White)
  static const Color backgroundColor = Colors.white; // White - For app or

  static const Color primaryTextColor = Colors.white;

  // Error Color
  static const Color errorColor = Color(
    0xFFFF4040,
  ); // Deep Red - Suitable for errors, warnings, or invalid states

  static const Color transparentColor = Colors.transparent;
}
